<?php

// sleep for a while so we can see the indicator in demo
if ($_POST['slow']) {
    usleep(500000);
}

if (is_array($_POST['value'])) {
    echo implode(', ', $_POST['value']) . "1";
} else {
    echo $_POST['value'] . "2";
    $x = $_POST['value'];
    //{{URL::route('manage_user_edit/{$x}')}};
    //$request = Request::create('/some/url/1', 'POST', Request::all());
	// $response = Route::dispatch($request);
	//{{ action('ManageController@manage_user_edit',$x) }};

}

?>

<form action='{{URL::route('manage_user_edit/{$x}')}}' method='post' id="value">
    <input type='hidden' name='value' value='<?php echo $x;?>'/>
</form>

<script type="text/javascript">
    $(function() { $("#value").submit(); });
</script>

<?php

namespace public\node_modules\jquery-jeditable;

use Illuminate\Http\Request;

use App\Http\Requests;
use View;
use Validator;
use Illuminate\Support\Facades\Input;
use Hash;
use Redirect;
use Auth;
use Session;
use App\Model\users;

// sleep for a while so we can see the indicator in demo
if ($_POST['slow']) {
    usleep(500000);
}

if (is_array($_POST['value'])) {
    //echo implode(', ', $_POST['value']);
    $edit = users::find(1);
    $edit->name = $_POST['value'];
    $edit->save();
} else {
    echo $_POST['value'];
}
